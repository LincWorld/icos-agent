{
	"name": "icosagent",
	"version": "2.0.1",
	"main": "index.ts",
	"private": true,
	"scripts": {
		"build": "bun build ./index.ts --compile --target bun --outfile icosagent",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "Apache-2.0",
	"description": "",
	"dependencies": {
		"mqtt": "5.13.2"
	},
	"devDependencies": {
		"@types/bun": "^1.2.18"
	}
}
